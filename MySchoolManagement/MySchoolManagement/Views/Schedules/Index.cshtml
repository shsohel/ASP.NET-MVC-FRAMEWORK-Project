@model IEnumerable<MySchoolManagement.Models.Schedule>

@{
    ViewBag.Title = "Index";
    Layout = "~/Views/Shared/_DashboardLayout.cshtml";

}

<h2>Index</h2>

<p class="btn btn-group">
    @Html.ActionLink("Create New", "Create" )
</p>
<table class="table table-bordered table-responsive ">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Teacher.TeacherName)
        </th>

        <th>
            @Html.DisplayNameFor(model => model.Teacher.TeacherName)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Teacher.TeacherName)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.ClassScheduleID }) |
            @Html.ActionLink("Details", "Details", new { id=item.ClassScheduleID }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.ClassScheduleID })
        </td>
    </tr>
}

</table>
